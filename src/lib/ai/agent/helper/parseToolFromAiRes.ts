/**
 * @deprecated This file is deprecated. Tool calls are now handled via OpenAI's native tool calling feature.
 * Tool calls are returned directly from the API response in the `tool_calls` field.
 * See: src/lib/ai/agent/tools/toolSchema.ts for the new tool definitions.
 */

// This file is kept for backwards compatibility but is no longer used.
// The parseMessageWithToolUse function has been replaced by OpenAI's native tool calling.

export {};