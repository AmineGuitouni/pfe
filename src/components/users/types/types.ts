export type User = {
    id: string;
    email: string;
    first_name: string;	
    last_name: string;
    country: string;
    phone_number: string;
    created_at: string;
    group : string
}

export type GroupSelected = {
    id: string;
    name: string;
}